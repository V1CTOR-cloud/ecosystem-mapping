import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import moment from "moment";

import { withChakra } from "../../../.storybook/preview";
import ListMap from "../../components/dashboard/mapView/ListMap";

<Meta
  title="Mapping App/Dashboard/Map view"
  component={ListMap}
  decorators={[withChakra]}
/>

# List View

export const ListTemplate = (args) => <ListMap {...args} />;

<Canvas>
  <Story
    name="List"
    args={{
      data: {
        id: "",
        mapStatus: "",
        title: "Title",
        owner: {
          profileName: "Mickaël Benasse rzer zer ez",
        },
        created: moment().format("DD-MM-YYYY"),
        lastModification: moment().format("DD-MM-YYYY"),
        image: "",
        location: {
          continent: "Europe",
          country: "Finland",
          region: "",
          city: "",
        },
        industry: {
          mainIndustry: "Information Technology",
          subIndustry: " rze z er rez ",
        },
        services: []
      },
    }}
  >
    {ListTemplate.bind({})}
    }} />
  </Story>
</Canvas>

<ArgsTable story="List" />