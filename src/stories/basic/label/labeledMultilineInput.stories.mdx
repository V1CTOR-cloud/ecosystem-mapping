import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";
import { withChakra } from "/.storybook/preview";
import LabeledMultilineInputComponent from "../../../components/basic/inputs/input/multilineInputComponent/LabeledMultilineInputComponent";

<Meta
  title="Mapping App/Basic/Label"
  component={LabeledMultilineInputComponent}
  decorators={[withChakra]}
/>

# Label with tooltip

<Canvas>
  <Story
    name="Multiline input with tooltip"
    args={{
      label: "Label",
      tooltipText: "Tooltip",
      tooltipAriaLabel: "Label",
      initialValue: "",
      placeholder: "Placeholder",
      onChange: () => {},
    }}
  >
    {(args) => <LabeledMultilineInputComponent {...args} />
    }
  </Story>
</Canvas>

<ArgsTable story="Multiline input with tooltip" />