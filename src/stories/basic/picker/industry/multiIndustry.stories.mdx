import { ArgsTable, Canvas, Meta, Story } from "@storybook/addon-docs";

import { withChakra } from "../../../../../.storybook/preview";
import { Industry } from "/src/service/industry";
import MultiIndustryPicker from "../../../../components/basic/picker/industryPicker/MultiIndustryPicker";

<Meta
  title="Mapping App/Basic/Industry/Multiple Industry"
  component={MultiIndustryPicker}
  decorators={[withChakra]}
/>

# Multiple industry

The component displayed is a multiple industry selection, we can then select more than one industry.

## Without initials values

<Canvas>
  <Story
    name="Without initials values"
    height="200px"
    inline={false}
    loaders={[
      async () => ({
        industries: await ((await Industry.getAllIndustries()).data.industries),
      }),
    ]}
  >
    {(args, { loaded: { industries } }) => <MultiIndustryPicker
      {...args}
      industriesList={industries}
      onChange={() => {
      }}
      initialValues={[{
        mainIndustry: null,
        subIndustry: null,
      }]}
    />}
  </Story>
</Canvas>

<ArgsTable story="Without initials values" />

## With initials values

### With only one value

<Canvas>
  <Story
    name="With one initial values"
    height="200px"
    inline={false}
    loaders={[
      async () => ({
        industries: await ((await Industry.getAllIndustries()).data.industries),
      }),
    ]}
  >
    {(args, { loaded: { industries } }) => <MultiIndustryPicker
      {...args}
      industriesList={industries}
      onChange={() => {
      }}
      initialValues={[{
        mainIndustry: "Aerospace and Defense",
        subIndustry: "Aerospace Products and Parts",
      }]}
    />}
  </Story>
</Canvas>

<ArgsTable story="With one initial values" />

### With multiple values

<Canvas>
  <Story
    name="With initials values"
    height="200px"
    inline={false}
    loaders={[
      async () => ({
        industries: await ((await Industry.getAllIndustries()).data.industries),
      }),
    ]}
  >
    {(args, { loaded: { industries } }) => <MultiIndustryPicker
      {...args}
      industriesList={industries}
      onChange={() => {
      }}
      initialValues={[{
        mainIndustry: "Aerospace and Defense",
        subIndustry: "Aerospace Products and Parts",
      }, {
        mainIndustry: "Automotive",
        subIndustry: null,
      }]}
    />}
  </Story>
</Canvas>

<ArgsTable story="With initials values" />